
  # Organize file structure

  This is a dashboard, designed in Figma and coded with React.
  
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
